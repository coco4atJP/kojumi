# Examples

The main reference examples currently live in:

- `scripts/simulate_agent_worker.py`: minimal worker flow
- `scripts/evaluator_daemon.py`: evaluator daemon example
- `mcp_servers/kojumi_eval_mcp`: MCP-based evaluation submission

These examples are intentionally small. They demonstrate the Kojumi API flow and
should be adapted before production use.
